# 小盈工作区跨机器 Git 同步方案

**目标**: 在多台电脑间同步小盈的完整工作区（包括历史对话、技能、定时任务、记忆）

---

## 📦 当前状态

**本地仓库**:
- ✅ Git 已初始化
- ✅ 首次提交完成：2,490 个文件
- ✅ 远程仓库已设置：`https://github.com/Hank-zz1/xiaoying-workspace.git`
- ⚠️ 等待首次推送到 GitHub

---

## 🚀 完整流程

### 阶段 1: 在当前电脑推送数据到 GitHub

#### 步骤 1.1: 确保能访问 GitHub

**测试连接**:
```bash
ping github.com
```

如果无法访问，需要:
- 开启代理/VPN
- 或使用 GitHub Desktop (会自动处理网络)

#### 步骤 1.2: 推送到 GitHub

**方式 A: 使用命令行**
```bash
cd C:\Users\26011970\.incaier-agent\workspaces\my-workspace

# 设置凭证管理器
git config --global credential.helper wincred

# 推送
git push -u origin main
```

Windows 会弹出登录窗口:
- 账号：932142751@qq.com
- 密码：Liu112233
- 或使用 Personal Access Token

**方式 B: 使用 GitHub Desktop (推荐)**
1. 下载：https://desktop.github.com/
2. 登录 GitHub 账号
3. File → Add Local Repository
4. 选择：`C:\Users\26011970\.incaier-agent\workspaces\my-workspace`
5. 点击 "Push origin"

#### 步骤 1.3: 验证推送成功

访问：https://github.com/Hank-zz1/xiaoying-workspace

应该能看到所有文件（2,490 个文件）

---

### 阶段 2: 在新电脑拉取并恢复工作区

#### 步骤 2.1: 在新电脑安装必要软件

1. **安装 Git**
   - 下载：https://git-scm.com/download/win
   - 安装时保持默认选项

2. **安装小盈 Agent**
   - 下载最新版本
   - 完成安装

3. **安装 GitHub Desktop (可选但推荐)**
   - 便于后续同步管理

#### 步骤 2.2: 克隆仓库

**方式 A: 使用命令行**
```bash
# 创建工作区目录
mkdir C:\Users\<新电脑用户名>\.incaier-agent\workspaces
cd C:\Users\<新电脑用户名>\.incaier-agent\workspaces

# 克隆仓库
git clone https://github.com/Hank-zz1/xiaoying-workspace.git my-workspace
```

**方式 B: 使用 GitHub Desktop**
1. File → Clone Repository
2. 选择 URL 标签
3. 输入：`https://github.com/Hank-zz1/xiaoying-workspace.git`
4. Local Path: `C:\Users\<用户名>\.incaier-agent\workspaces\my-workspace`
5. 点击 Clone

#### 步骤 2.3: 在 Agent 中关联工作区

1. 打开小盈 Agent
2. 设置 → 工作区
3. 选择 "打开现有工作区"
4. 选择：`C:\Users\<用户名>\.incaier-agent\workspaces\my-workspace`
5. 重启 Agent

#### 步骤 2.4: 验证数据完整性

检查以下内容是否完整:
- [ ] 历史会话 (应该显示 285 个)
- [ ] 技能列表 (应该显示 197 个)
- [ ] 定时任务 (应该显示 6 个)
- [ ] 长期记忆 (应该显示 370+ 条)

---

## 🔄 日常同步流程

### 电脑 A (工作并提交)

```bash
# 1. 完成工作后
cd C:\Users\<用户名>\.incaier-agent\workspaces\my-workspace

# 2. 查看更改
git status

# 3. 添加更改
git add -A

# 4. 提交
git commit -m "描述你的更改，例如：添加新的冰箱监控会话"

# 5. 推送
git push
```

### 电脑 B (拉取更新)

```bash
# 1. 打开工作区前，先拉取更新
cd C:\Users\<用户名>\.incaier-agent\workspaces\my-workspace

# 2. 拉取最新代码
git pull

# 3. 打开 Agent 继续工作
```

### 双向同步最佳实践

**每天工作流程**:
```
早上 (电脑 A):
  1. git pull (拉取昨晚电脑 B 的更改)
  2. 开始工作
  
下班前 (电脑 A):
  1. git add -A
  2. git commit -m "今日工作"
  3. git push
  
晚上 (电脑 B):
  1. git pull (拉取电脑 A 的更改)
  2. 继续工作
  
睡觉前 (电脑 B):
  1. git add -A
  2. git commit -m "晚间工作"
  3. git push
```

---

## ⚠️ 重要注意事项

### 1. 大文件处理

**当前仓库大小**: 约 300MB+

如果继续增长，建议:

**方案 A: 使用 Git LFS**
```bash
# 在任一台电脑执行
git lfs install
git lfs track "sessions/*/attachments/*"
git lfs track "sessions/*/data/*"
git add .gitattributes
git commit -m "Configure Git LFS for large files"
git push
```

**方案 B: 排除大型附件**
编辑 `.gitignore`，添加:
```
# 排除大型会话数据
sessions/*/attachments/*
sessions/*/downloads/*
data/*.pdf
data/*.zip
```

然后:
```bash
git rm -r --cached sessions/*/attachments
git rm -r --cached sessions/*/downloads
git commit -m "Remove large attachments from git"
git push
```

### 2. 冲突解决

如果两台电脑同时修改了同一个文件:

```bash
# 拉取时遇到冲突
git pull

# 查看冲突文件
git status

# 编辑冲突文件，解决冲突标记 (<<<<<<, ======, >>>>>>)

# 标记为解决
git add <文件名>

# 完成合并
git commit -m "解决合并冲突"
```

### 3. 敏感信息

**不要提交**:
- API 密钥
- 数据库密码
- 私人密钥

如果已提交，需要重写历史:
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch 敏感文件路径" \
  --prune-empty --tag-name-filter cat -- --all
git push --force
```

---

## 📊 优化建议

### 1. 定期清理旧会话

```bash
# 只保留最近 3 个月的会话
# 手动删除旧会话文件夹
git add -A
git commit -m "清理旧会话数据"
git push
```

### 2. 使用分支管理 (进阶)

```bash
# 创建开发分支
git checkout -b dev

# 在 dev 分支测试新功能
git add -A
git commit -m "测试新功能"
git push origin dev

# 测试完成后合并到 main
git checkout main
git merge dev
git push
```

### 3. 自动同步脚本

创建 `sync.bat`:
```batch
@echo off
cd /d "C:\Users\%USERNAME%\.incaier-agent\workspaces\my-workspace"
echo 正在拉取最新数据...
git pull
echo.
echo 正在推送本地更改...
git add -A
git commit -m "自动同步"
git push
echo.
echo 同步完成！
pause
```

---

## 🔧 故障排查

### Q1: 推送失败 - "rejected"
**原因**: 远程仓库有本地没有的提交

**解决**:
```bash
git pull --rebase
git push
```

### Q2: 拉取失败 - "unrelated histories"
**原因**: 新电脑上创建了新的 Git 仓库

**解决**:
```bash
git pull --allow-unrelated-histories
```

### Q3: 文件太大无法推送
**解决**:
```bash
# 使用 Git LFS
git lfs install
git lfs track "*.pdf"
git add .gitattributes
git commit -m "Setup LFS"
git push

# 或排除大文件
echo "*.pdf" >> .gitignore
git rm --cached *.pdf
git commit -m "Remove large PDFs"
```

### Q4: 认证失败
**解决**:
```bash
# 清除保存的凭证
git credential-manager erase

# 或使用 Token 代替密码
# https://github.com/settings/tokens
```

---

##  快速参考卡

### 首次设置
```bash
# 当前电脑 (推送)
cd 工作区路径
git push -u origin main

# 新电脑 (拉取)
git clone https://github.com/Hank-zz1/xiaoying-workspace.git 工作区路径
```

### 日常同步
```bash
# 提交更改
git add -A
git commit -m "说明"
git push

# 拉取更新
git pull
```

### 检查状态
```bash
git status      # 查看更改
git log --oneline  # 查看历史
git remote -v   # 查看远程地址
```

---

## ✅ 总结

**当前进度**:
- ✅ Git 仓库已初始化
- ✅ 首次提交完成 (2,490 个文件)
- ✅ 远程仓库已设置
-  等待首次推送

**下一步**:
1. 在当前电脑推送到 GitHub
2. 在新电脑克隆仓库
3. 在 Agent 中关联工作区
4. 开始跨机器同步工作！

**仓库地址**: https://github.com/Hank-zz1/xiaoying-workspace

---

需要帮助？查看 `推送 GitHub 指南.md` 获取详细推送步骤。
