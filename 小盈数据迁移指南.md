# 小盈历史对话数据迁移指南

## 📦 需要迁移的核心数据

### 1. 历史对话数据 (最重要)
**路径**: `sessions/` 目录下的所有会话文件夹

每个会话文件夹包含:
- `session.jsonl` - 完整的对话记录 (核心文件)
- `session.wal.jsonl` - 写入日志
- `operation-journal.jsonl` - 操作日志
- `attachments/` - 附件文件
- `data/` - 会话数据文件
- `plans/` - 执行计划

### 2. 工作区配置
- `config.json` - 工作区基础配置
- `automations.json` - 自动化任务配置
- `memory/` - 长期记忆数据
- `skills/` - 工作区自定义技能
- `sources/` - 自定义数据源配置

### 3. 全局数据
- `C:\Users\26011970\.incaier-agent\skills\` - 全局技能
- `C:\Users\26011970\.incaier-agent\workspaces\my-workspace\skill-registry.json` - 技能注册表

---

## 🚀 迁移步骤

### 方法一：使用迁移脚本 (推荐)

我已经为你创建了迁移脚本，位于工作区根目录。

**在旧机器上执行**:
1. 右键点击 `迁移小盈数据.bat`
2. 选择"以管理员身份运行"
3. 等待打包完成 (会在工作区生成 `小盈数据备份_时间戳.zip`)

**在新机器上**:
1. 安装 Incaier Agent
2. 解压备份文件到新机器
3. 将工作区文件夹放到正确位置，或在 Agent 中重新关联

### 方法二：手动复制

**复制以下文件夹到新机器**:
```
C:\Users\26011970\.incaier-agent\workspaces\my-workspace\
├── sessions/          (历史对话 - 必选)
├── config.json        (工作区配置 - 必选)
├── automations.json   (自动化配置 - 推荐)
├── memory/            (长期记忆 - 推荐)
├── skills/            (自定义技能 - 推荐)
└── sources/           (数据源配置 - 推荐)
```

---

## ⚠️ 注意事项

1. **API 密钥安全**: 
   - 如果 `config.json` 或 `sources/` 中包含敏感 API 密钥
   - 建议在新机器上重新配置，不要直接复制

2. **路径一致性**:
   - 确保新机器上的用户目录路径相同
   - 或更新配置文件中的路径引用

3. **数据源认证**:
   - 部分数据源可能需要重新 OAuth 认证
   - 迁移后检查数据源连接状态

4. **技能依赖**:
   - 某些技能可能依赖本地文件路径
   - 检查并更新路径配置

---

## ✅ 迁移验证

迁移完成后检查:
- [ ] 历史会话是否正常显示
- [ ] 技能和自动化任务是否正常加载
- [ ] 数据源连接状态是否正常
- [ ] 长期记忆是否完整

---

## 📞 需要帮助?

如果迁移过程中遇到问题，请提供:
1. 旧机器和新机器的操作系统版本
2. 使用的迁移方法 (脚本/手动)
3. 具体的错误信息或异常现象

---

## 📦 当前备份位置

**最新备份**: `C:\Users\26011970\Desktop\小盈数据备份\小盈数据备份_20260921_111013\`

注意：由于 Windows 编码问题，文件夹名称可能显示为乱码"灏忕泩鏁版嵁澶唤_20260921_111013"，但实际内容已正确备份。

**备份内容验证**:
- ✅ `sessions/` - 所有历史会话数据
- ✅ `memory/` - 长期记忆
- ✅ `skills/` - 自定义技能
- ✅ `skills-dev/` - 技能开发草稿
- ✅ `sources/` - 数据源配置
- ✅ `config.json` - 工作区配置
- ✅ `automations.json` - 自动化配置
- ✅ `小盈数据迁移指南.md` - 本迁移指南

---
